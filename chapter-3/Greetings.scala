// Example of Scala, it does not require class or main method. It does not require semi colors or types. It knows how to infer the type. It also removes extra typing as we do not needs System.out for call print or println.
for(i <- 1 to 3) {
  print(i + ",")
}

println("Scala Rocks!!!")
